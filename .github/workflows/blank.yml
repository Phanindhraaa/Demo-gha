name: Hello World
on: 
  push:
    branches:
      - main
      - develop
      - 'release/**'
  pull_request:
    branches:
      - main    
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build phase
        run: echo "Running Build"
  
  
  
         
    
